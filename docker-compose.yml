version: '3.9'

services:
  moyin-creator:
    image: ghcr.io/qiaowenzeng99-tech/moyin-creator:latest
    container_name: moyin-creator
    restart: unless-stopped
    ports:
      - "8080:80"
    # 可按需改成固定版本，例如：
    # image: ghcr.io/qiaowenzeng99-tech/moyin-creator:sha-<commit>
    # 或使用 work 分支镜像（若存在）：
    # image: ghcr.io/qiaowenzeng99-tech/moyin-creator:work
