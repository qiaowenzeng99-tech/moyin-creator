services:
  moyin-creator:
    image: ghcr.io/qiaowenzeng99-tech/moyin-creator:latest # public
    container_name: moyin-creator
    restart: unless-stopped
    ports:
      - "8080:80"
    # 可按需改成固定版本，例如：
    # image: ghcr.io/qiaowenzeng99-tech/moyin-creator:sha-<commit>
