version: '3.9'

services:
  moyin-creator:
    image: ghcr.io/memecalculate/moyin-creator:work
    container_name: moyin-creator
    restart: unless-stopped
    ports:
      - "8080:80"
    # 若你希望在本地直接使用源码构建，可改为：
    # build:
    #   context: .
    #   dockerfile: Dockerfile
