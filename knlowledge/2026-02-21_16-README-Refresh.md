# 2026-02-21 16: README 文档更新

## 本次更新目标
- 同步 README 与当前仓库归属、分支策略、部署方式。
- 补充 Canvas MVP 与 Docker 快速部署说明，降低新用户上手成本。

## 变更点
1. 仓库链接与克隆地址更新为 `qiaowenzeng99-tech/moyin-creator`。
2. 新增目录导航中的 `Docker 部署` 入口。
3. 功能特性新增 `Canvas AI 驱动交互（MVP）` 描述。
4. 新增 Docker 部署章节：`docker compose up -d` / `docker run` 示例。
5. 说明镜像标签发布策略已默认对齐 `main` 分支。

## 结果
- README 信息与当前代码和发布策略保持一致。
